add_library(lol_session_inbound INTERFACE)
target_include_directories(lol_session_inbound INTERFACE inbound/include)
target_link_libraries(lol_session_inbound INTERFACE lol_kernel)
lol_register(lol_session_inbound inbound session)
add_library(lol_session_outbound INTERFACE)
target_include_directories(lol_session_outbound INTERFACE outbound/include)
target_link_libraries(lol_session_outbound INTERFACE lol_kernel)
lol_register(lol_session_outbound outbound session)
add_library(lol_session_app STATIC src/ksessions.cpp)
target_link_libraries(lol_session_app PRIVATE lol_session_domain lol_session_inbound lol_session_outbound)
lol_register(lol_session_app app session)
