# 前端交互一致性检查与修正

2026-09-17。依据当前源码检查，未声称完成桌面像素级或键盘实机验收。

| 不合理之处 | 修改 |
|---|---|
| 大纲条目显示展开箭头，但点击实际只跳转 | 标题保留真实展开箭头，条目改为章节符号；当前章节添加 aria-current |
| 章节减少至五个后筛选框隐藏，但旧查询仍生效 | 有查询时始终保留输入框，允许清空；忽略查询首尾空白 |
| 项目为空和搜索无结果共用提示 | 分别提示新建文件或调整筛选 |
| 不同目录的同名文件标签难区分 | 出现同名时显示路径，长路径省略但 title 保留完整路径 |
| 标签缺少方向键切换 | 增加左右方向键、Home、End 与活动标签 tabIndex；切换时滚动到可见位置 |
| 菜单执行后焦点可能留在隐藏项 | 焦点仍在菜单内时返回触发按钮；编辑器等动作已转移焦点时不抢回 |
| 菜单可重叠且键盘浏览不便 | details 分组互斥；方向键、Home/End 导航，焦点移出时关闭 |
| 项目忙碌时部分编辑按钮仍可点击 | 撤销、重做、粗体与斜体同步禁用 |

验证：最终 `./scripts/build.ps1 --dev` 完整成功，TypeScript 与 git diff --check 通过。日志：out/verification/interactionbuild.log。未运行测试套件或桌面交互验收；未修改用户正在编辑的 support/examples/paper001/main.tex。

本次只处理以上明确问题，不代表前端全部功能完成审查。文件树完整方向键导航、菜单屏幕边缘定位、对话框不同 DPI 和 PDF 交互仍需后续专门验收。
