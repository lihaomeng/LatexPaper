# 工作台布局调整（2026-09-17）

## 已实现

- 顶部项目菜单、弹性项目标题与精简常用操作，文件操作移入侧栏。
- 默认 260px 文件侧栏；可拖动分栏；编辑优先、对照编辑、阅读优先预设；布局与缩放偏好保存在本机。
- 窄窗口使用抽屉侧栏，小于 760px 时通过布局预设切换源码和 PDF。
- 空大纲默认折叠，文件行提供悬停操作、已打开与未保存标记。
- PDF 支持适合宽度、整页、自定义比例、页码跳转，尺寸变化时重新渲染；保留候选 PDF 提交和 SyncTeX 坐标换算。
- 编译日志移动到底部，失败自动展开；状态栏汇总编译状态与耗时。
- 布局状态、顶部栏、大纲、日志与编译反馈拆为独立组件或 hook。

## 验证范围

- 使用 `scripts/build.ps1 --dev` 构建 Electron + C++ 后端，包含 TypeScript 检查。
- Playwright 浏览器人工检查 1440×900 和 560×760 布局、布局菜单、阅读预设及抽屉侧栏。
- 截图位于忽略目录 `output/playwright/`。
- 浏览器预览未连接原生后端，因此没有执行真实 TeX 编译、PDF 渲染或 SyncTeX 的端到端验收；未运行测试套件。
- 浏览器日志仅发现 favicon.ico 缺失的 404。

## 连续 PDF 页面增量

- PDF 全部页按实际尺寸纵向排列，保留页间距；页码输入与前后按钮滚动定位，不再替换单页画布。
- 滚动同步页码；首屏之外的页面仅在可视区域附近渲染，离开后释放画布位图。
- 保留适宽、整页、自定义缩放，以及每页独立的 SyncTeX 双击坐标与正向定位标记。
- 本次验证使用 `scripts/build.ps1 --dev`；未执行测试套件，连续 PDF 与 SyncTeX 的桌面交互仍待人工验收。
