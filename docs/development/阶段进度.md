# LightOverLeaf 阶段执行记录

更新日期：2026-09-13。

执行依据：[开发要求](LightOverLeaf_开发要求.md)第 22、23、26、27 节。阶段范围现为 M0 → M9。M0～M8 的历史执行与遗留项按原记录保留；M9 当前仅完成规划，尚未开始产品实现。历史原型不能替代对应阶段验收。

## 阶段总表

| 阶段 | 状态 | 验收重点与现有证据 |
| --- | --- | --- |
| M0 架构骨架 | 基线已验收（历史记录） | 显式 Target、架构检查、非法依赖失败与契约生成；见 [M0 记录](../acceptance/M0.md)。 |
| M1 Qt/CEF Shell | 基线已验收（历史记录） | 生命周期端口、Renderer 恢复与关闭路径；见 [M1 记录](../acceptance/M1-native-ping.md)。不代表原生文档恢复完成。 |
| M2 React 与资源 | 进行中／复验 | 用户已授权最小样式修复；CEF 高 DPI 客户区铺满缺陷已按原生尺寸修复并通过桌面 smoke，自动化界面工具故障导致最终截图待用户复验；见 [M2 记录](../acceptance/M2-draft-workbench.md)。 |
| M3 RPC Core | System 基线已验收 | 纯 C++ 端点、共用 Loopback/CEF 替换场景、真实跨进程取消与重载回归通过；见 [端点验收](../acceptance/M3-endpoint-substitution.md)。 |
| M4 Workspace/Document | 自动化实现完成／环境边界待验收 | 文件/目录管理、原生变化通知、安全刷新、冲突恢复、原子另存、带索引回收恢复和保守三方合并已贯通。真实 CEF 4 MiB 保存/读取往返已通过；特权文件系统夹具及桌面人工验收待补，见 [M4 增量](../acceptance/M4-document-core.md)。 |
| M5 Search/Build | 实现完成／真实 TeX 待验收 | Search、快照、latexmk 优先及直接引擎回退、进程树取消、超时、实时有界状态/日志和分级诊断已贯通；最新 Debug/Release 均为 33/33，见 [M5 记录](../acceptance/M5-search-build.md)。本机无 TeX，不声称真实论文编译通过。 |
| M6 Preview/Navigation | 自动化实现完成／真实 TeX 端到端待验收 | Artifact、分块 RPC、PDF.js、Windows SyncTeX 与独立 Unavailable Adapter 已贯通；路径不暴露前端，能力按实际命令发现动态报告，见 [M6 记录](../acceptance/M6-preview-navigation.md)。 |
| M7 Preferences/Session | 自动化验收完成／人工视觉待补 | 两个独立模块、SQLite Adapter、RPC、设置页、自动编译、完整布局/标签/光标/缩放恢复和最近项目已贯通；最新总回归 33/33 与前端 288/288 通过，见 [M7 记录](../acceptance/M7-preferences-session.md)。 |
| M8 发布 | Full 绿色目录本机验收完成／干净机与签名待补 | 唯一产品形态内置源码版 MiKTeX，运行时零解压；入口 GUI Subsystem、目录 smoke、真实 PDF/SyncTeX 和 33/33 回归均通过，见 [M8 记录](../acceptance/M8-release.md)。 |
| M9 托管草稿与实时预览 | 规划完成／尚未实现 | 已确认只有导出/另存为选择目标目录；Overlay Snapshot、Latest-wins、PDF 原子切换与独立 Export 边界见 [M9 记录](../acceptance/M9-live-preview.md)。 |

“基线已验收”引用既有验收记录，不表示本次重新执行了测试，也不免除后续修改的回归验证。

## 历史阶段：M2（未验收项保留）

### 已有成果

- 深色三栏布局、文件树、大纲、多标签和 Monaco 草稿编辑。
- 本地离线脚本、Monaco Worker 和字体打包；开发 Fake 与生产 CEF 通道分离。
- 草稿检查点串行写入、恢复和多窗口写入互斥；这些不是原生项目文件保存。
- 历史验证：前端 64 项测试、桌面 Debug／Release 各 11 项测试通过。具体环境、命令、范围及限制以 M2 验收记录为准。

### 修复前未通过项与所需决策（历史）

严格 CSP 仍阻止 Monaco 的内联样式属性，代码行发生重叠。已有随机 nonce 方案允许受控动态样式表，但未解决行定位属性；不得将“页面就绪”或测试通过当作视觉通过。

拟议修复需要用户明确允许仅增加 `style-src-attr 'unsafe-inline'`。这会降低样式属性注入防护；脚本限制、带 nonce 的样式表限制及桌面远程连接限制继续保留。此前该安全策略变更未获授权，本次“按阶段执行”不视为此项授权，因此尚未实施。

取得明确决策后，先完成 M2 修复及复验，再进入 M3。M2 剩余检查包括实际编辑器排版、中文输入、窗口缩放、离线 Worker／字体加载、浏览器独立测试及 CEF 桌面回归。PDF Worker 的实际接入与验收属于 M6，未接入前不声称已经验证。

### 授权后的当前状态

2026-09-11 用户明确回复“可以”。两处 CSP 已仅增加 `style-src-attr 'unsafe-inline'`，其余限制保留。新增安全回归测试，本次前端 65/65、桌面双配置构建及各 11/11 CTest 通过；浏览器 1600×1000 与 700×800 截图确认排版正常，中文草稿重载恢复通过。详见 M2 验收记录末尾。

授权阻塞已解除。M2 仍需 CEF 桌面实际视觉／中文 IME、Worker 执行及离线加载证据、查找和撤销重做复验。用户随后明确要求继续 M3，已据此调整当前阶段，未将 M2 增量修复记为完整阶段完成。

## 本次执行日志

### 2026-09-12：遗留功能收口第 1 阶段（编辑／预览联动）

- 已将“自动编译”从仅持久化偏好改为保存成功后 900 ms 防抖触发；打开项目不会误触发，冲突、保存中和已有编译任务时不会并发启动。
- 已接通源码光标到 PDF 的正向 SyncTeX，PDF 自动切页、滚动并显示定位标记；反向 SyncTeX 坐标按 PDF 实际缩放比例还原。
- PDF.js 预览新增 50%～300% 缩放和一键恢复 100%，移除会限制真实画布缩放的 CSS。
- 更新帮助与设置文案，不再错误声称缺少 SyncTeX Backend。
- 本次实际执行 `npm.cmd run check`：ESLint 通过，前端 288/288 通过，TypeScript 与 Vite 生产构建通过。
- 真实 `synctex.exe` 映射仍需 TeX 环境端到端验证；本阶段仅声明代码与自动测试通过。

### 2026-09-12：遗留功能收口第 2 阶段（搜索／Artifact 生命周期）

- Search Application 现在同时产生文件名和正文命中；文件名命中使用显式 `[文件名]` 预览，不把该规则塞入 LocalFS Adapter 或 React。
- Local Artifact Store 新增不透明 ID 校验、并发访问互斥、启动及发布后的缓存治理；最多保留 20 个产物目录并限制为 512 MiB，始终优先保留当前产物，拒绝路径穿越 ID。
- 淘汰只处理缓存根下合法、非 Reparse Point 的产物目录，不递归跟随链接。
- 本次实际构建 `LightOverLeafSearchTests`、`LightOverLeafPreviewTests`，CTest 2/2 通过；新增 25 次发布淘汰与非法 Artifact ID 回归。

### 2026-09-12：遗留功能收口第 3 阶段（项目创建／完整会话）

- 顶部菜单新增“新建项目”：继续使用 Qt 提供的受信任目录选择，不向 React 开放绝对路径；只接受空目录，并通过原子的 `document.saveAs` 创建独立、可直接编译的 `main.tex`。
- Session RPC 与 Application DTO 新增编辑区宽度、活动光标行列和 PDF 缩放；前端在 500 ms 防抖保存后恢复分栏、预览开关、缩放、活动标签与光标。
- SQLite Adapter 使用 `PRAGMA table_info` 检测并 `ALTER TABLE` 增量迁移旧库；空工作区不再进入最近项目历史。
- 新增完整会话契约 Fixture，V2 契约由 158 增至 160；前端 ESLint、287/287、TypeScript、Vite 均通过。
- Release 下 `LightOverLeafProtocolTests`、`LightOverLeafPersistenceTests`、`LightOverLeafWorkspaceWorkflowRpcTests` 共 3/3 通过。
- 最近项目仍保存为不透明工作区标识；出于路径权限边界，重新打开仍要求用户通过原生选择器确认目录。

### 2026-09-12：遗留功能收口第 4 阶段（生产编译链）

- Windows Build Adapter 在所选 TeX 根、TeX Live/MiKTeX 常用子目录及 PATH 中发现 `latexmk.exe`；当目标引擎同时可用时优先通过 `-pdf`、`-pdfxe` 或 `-pdflua` 执行，由 latexmk 负责多轮编译以及 BibTeX/Biber 编排。
- 未安装 latexmk 时继续使用原来的安全直接引擎回退；两条路径都保持参数数组语义、禁用 Shell Escape、启用 SyncTeX、受 Job Object 进程树取消和统一超时约束。
- 结构化诊断现在区分 `error`、`warning`、`info`，并补充传统 `! LaTeX Error` 与包警告解析，不再把所有日志统一标为错误。
- Fake EXE 黑盒新增实际 launcher 识别和 warning 分类；Release `LightOverLeafBuildTests` 1/1 通过。
- 编译仍由一次可取消异步请求返回终态；第 5 阶段已追加独立的有界状态查询，因此无需引入无背压的逐行推送事件。

### 2026-09-12：遗留功能收口第 5 阶段（实时有界编译状态）

- Build Application 新增独立 `status(jobId)` 入站能力；运行中状态保存最多 1 MiB 的 UTF-8 安全日志，终态状态仅保留最近 20 个任务，避免常驻内存无界增长。
- Windows Compiler Adapter 在读取匿名管道时增量回调；Build Application 负责状态生命周期，Adapter 不依赖 RPC 或 React，继续保持端口隔离。
- RPC v2 新增严格的 `build.status` 请求/响应；未知字段、非法任务 ID 和不存在任务均显式拒绝。前端每 200 ms 查询当前任务，完成或异常后必定清理定时器。
- 自动测试覆盖运行中日志、成功/取消终态保留、未知任务、RPC 投影和 TypeScript 客户端校验；共享 V2 Fixture 由 160 增至 163。
- 本次实际执行 `npm.cmd run check`：ESLint、前端 287/287、TypeScript、Vite 生产构建全部通过；随后完整 `desktop-release` 构建通过，CTest 31/31 通过（32.32 秒）。
- 该设计提供实时可见且有界的日志快照，不承诺逐行事件推送；真实 TeX 输出节奏仍需具备 TeX 的环境复验。

### 2026-09-12：遗留功能收口第 6 阶段（性能基线／CEF 极值往返）

- 新增独立 `LightOverLeafPerformanceTests`，在真实 LocalFS Adapter 上测量 1000 文件树扫描、项目搜索、文件打开和原子保存；测试数据仅位于自建临时目录。
- Release 实测：1000 文件树 61.177 ms、1000 文件搜索 333.514 ms、普通文件打开 0.266 ms、原子保存 10.046 ms；全部满足当前自动化预算。
- Release 可执行文件连续三次完整进程 smoke 为 1377.48 ms、1252.47 ms、1262.27 ms。该值包含页面就绪后的 250 ms 安全延迟，是热缓存整进程上界，不冒充严格冷启动或首帧可交互测量。
- 新增 `--smoke-test-payload`：真实 CEF Browser/Renderer 经生产 RPC 将恰好 4 MiB UTF-8 文本保存到隔离工作区、再次读取并逐字节比较；测试通过且临时目录清理完成。
- 编译日志上限补充非法 UTF-8 扩张场景：清洗后的有效 UTF-8 状态仍不超过 1 MiB，Fake EXE 黑盒覆盖该边界。
- 测试设备：Intel Core Ultra 9 275HX，24 核/逻辑处理器，31.7 GiB 内存，Windows 11 家庭中文版 10.0.26200（Build 26200）；构建类型为 Release。
- 最新完整 `desktop-release` 与 `desktop-debug` 均为 33/33 通过；前端仍为 287/287，架构检查为 86 个显式 Target。
- 可见 CEF 界面与中文 IME 自动化因本轮桌面控制运行时启动失败未完成；特权 Reparse Point、极端长路径、崩溃中断、真实冷启动和真实 TeX 端到端仍作为明确的环境验收项。详细证据见 [性能与遗留收口](../acceptance/性能与遗留收口.md)。

### 2026-09-12：M8 Lite 单文件发布

- 新增目录保真的两层单文件打包：IExpress 外壳 + 7z Runtime 载荷；不再使用会压平 CEF 子目录的直接封装。
- 最新修复版 EXE：`out/packages/lite-20260913-103955-6ca2ef54/LightOverLeaf-0.1.0-lite-win64.exe`。
- 大小 152023040 bytes；SHA-256：`08FE4838723C1865F4528735593466583AFEC0A89894FCA12AD4E04B7A2255EC`；打包入口重新执行 Release 33/33，归档测试和直接外层 EXE smoke 均通过。
- Full 脚本已具备，但本机无 Portable TeX，按规则记录阻塞，不自动下载、不伪造包。

### 2026-09-13：桌面外壳缺陷修复（无命令行窗口／CEF 铺满）

- 已复现用户截图中的两个问题：旧单文件包由 IExpress 执行 `run.cmd`，会显示 `cmd.exe`；Qt 高 DPI 环境把逻辑尺寸直接传给 CEF 子 HWND，导致页面只占客户区左上部分。
- 新增 `LightOverLeafRuntimeLauncher`：Windows GUI 子系统、静态 MSVC Runtime，只依赖 `KERNEL32.dll`/`USER32.dll`。它以 `CreateProcessW` 隐藏运行 7-Zip、正常启动 GUI 主程序、等待退出并清理随机临时目录；失败使用原生错误窗口，不经过 Shell。
- 单文件外层只包含 `payload.7z`、`7z.exe`、`7z.dll` 和原生 GUI 启动器，已移除 `run.cmd`。打包脚本解析 PE 头，拒绝 Console 子系统，并实际运行最终外层 EXE smoke。
- CEF Platform 在创建和缩放浏览器子窗口时通过父 HWND 的 `GetClientRect` 获取原生客户区尺寸，不再使用 Qt 高 DPI 逻辑像素作为 Win32 子窗口像素。
- 架构检查通过，共 87 个显式 Target；受影响 CEF/Qt/架构测试 9/9，通过后完整 Release 33/33，268 文件归档测试和最终包 smoke 均通过。
- 使用 `computer-use` 尝试可见截图复验两次，均因 Codex Windows sandbox helper 启动失败而中止；因此代码、PE 结构和运行 smoke 已验证，但最终可见截图仍需用户打开新包确认，不冒充人工视觉通过。

### 2026-09-12：M7 Preferences/Session

- 新增两个独立模块及 SQLite Adapter；RPC 只依赖 Inbound Port，SQLite 不进入业务层。
- 设置页、编译引擎/超时、TeX 根目录、布局、标签恢复和最近项目历史已贯通。
- 前端 287/287、最新 Debug/Release 均为 33/33；可见桌面设置操作的人工复验仍保留。

### 2026-09-12：M6 Preview/Navigation

- Build 通过发布端口交付 PDF／SyncTeX 字节，Preview 以不透明 Artifact 和有界分块向前端提供 PDF。
- PDF.js 与离线 Worker 已接入；Windows SyncTeX Adapter 使用受限子进程完成正反向查询，Basic Fake 只存在测试组合。
- Fake `synctex.exe` 黑盒覆盖成功、非零退出、非法/超量输出、5 秒超时、进程树终止与临时目录清理；生产无命令时改用独立 Unavailable Adapter。
- 最新前端 287/287、Debug/Release 均为 33/33。本机无 TeX，因此当前运行能力为 `syncTex=false`，真实论文端到端保留为外部环境验收。

### 2026-09-12：M5 Search/Build

- 新增独立 Search 与 Build Domain/Application/Adapter Target，RPC v2 契约与 React NativeApi。
- Windows Build 后端以快照运行 Fake 编译器 EXE，验证成功、失败、非法输出、超时、取消及清理。
- 修复 CEF Web 资源部署依赖，完整 `desktop-release` 27/27 通过；前端 284/284 通过。
- 本机没有 TeX，真实 TeX 验收保留；阶段实现已具备进入 M6 的条件。

### 2026-09-13：PDF 编译入口修复与 M5 复验

- 修复截图中草稿态编译按钮不可用：按钮改为“保存并编译”，使用原生目录选择器把当前多文件草稿（含嵌套目录）写入空项目，再自动执行现有 Snapshot → TeX → Artifact → PDF.js 流程。
- 新增 Build Outbound `IKCompilerRootSource`，通过 Composition Bridge 读取 Preferences Inbound；Build Application、Windows Adapter 与 Preferences/SQLite 实现保持解耦。TeX Root 修改后在当前项目下一次编译即时生效。
- Windows 探测覆盖应用目录随包 `texlive`、`C:/texlive/<年份>`、MiKTeX、TinyTeX、显式 Root 与 PATH；直接子目录检查限制为 64，不递归扫描整盘。
- 未发现真实 TeX 时提供“配置 TeX 工具链”入口并保留明确不可用状态，不伪造 PDF、不自动下载宏包。
- 实际验证：前端 ESLint、288/288、TypeScript、Vite 通过；Release Desktop 完整构建通过；CTest 33/33 通过；新版 Lite 单文件的归档测试和直接 EXE smoke 通过。
- 新包：`out/packages/lite-20260913-112357-e171003e/LightOverLeaf-0.1.0-lite-win64.exe`；152023040 bytes；SHA-256 `C099C007E73E0EBBA162BB13122E3BC16A6BB59ABF6F927049588FAECB1D3535`。
- 环境边界：本机及 `D:/CodeMyself/QTBest/thirdparty_install` 仍没有真实 TeX。代码链路与 Fake 进程产物已验收，真实论文、宏包、字体、BibTeX/Biber 和 SyncTeX 仍需实际 TeX 工具链复验。

### 2026-09-13：MiKTeX 源码 Runtime 集成

- 固定官方 MiKTeX `26.5` 源码归档及 SHA-256 `a1fd2223ef04a0268b0718536f7b55a04b5c9d1f0f721d1c0b2749b3af95115d`，源码与构建树只位于被忽略的 `out/dependencies/miktex`。
- 新增 `build-miktex-runtime.ps1`，负责校验、配置、Release 构建、隔离安装、显式宏包准备、恢复源码二进制和关闭运行时自动安装；第三方安装根为 `D:/CodeMyself/QTBest/thirdparty_install/miktex`。
- 新增 `package-miktex.ps1`；统一 Full 入口要求 MiKTeX 与 Portable TeX Live 载荷二选一，MiKTeX 固定放置到 `runtime/miktex`。
- Composition Root 同时把应用旁和当前目录的 `runtime/miktex` 注入 Build/Navigation；Adapter 仍实现统一 `IKCompilerBackend`，但能力报告可区分 `toolchainId=miktex`。
- 首次 Boost.Locale 构建因中文用户名临时路径乱码失败；改用仓库内 ASCII `out/tmp/vcpkg` 后安装成功。源码兼容补丁解决 Windows SDK 19041 下 Shlwapi 声明和 Cairo DirectWrite API 差异；源码 Release 引擎与独立安装器构建成功。
- 源码版安装器已准备 `basic` 宏包、初始化 `texmfs/install` 官方便携布局、关闭自动安装；来源清单记录 `builtFromSource=true`。Windows Adapter 与打包器同时兼容开发安装布局和官方便携布局。
- 真实 XeLaTeX 生成 5172-byte PDF 与 520-byte SyncTeX；前端 288/288、Release CTest 33/33 通过。
- Full GUI 单文件 `out/packages/full-20260913-142310-3a4e46a2/LightOverLeaf-0.1.0-full-win64.exe` 已生成：426319872 bytes，SHA-256 `B039018F60A376DF935B4F0143BA5BF4911E48E89BC121B9B561FF06776306B7`；归档、GUI Subsystem 和直接 smoke 均通过。
- 尚未完成独立干净 Windows 虚拟机人工编辑/编译/预览验收；`basic` 集合也不承诺完整论文宏包闭包，这两项不伪造为通过。
- 设计、命令和验收边界见 [MiKTeX 源码构建与集成](MiKTeX源码构建与集成.md)。

### 2026-09-13：Full 单文件烟测超时修复

- 用户自行打包时 7z 对 19564 个文件、约 1.44 GB 载荷验证成功，但固定 45 秒的最终 EXE smoke 超时。
- 同一 EXE 使用延长上限复验为 46735 ms、退出码 0，确认属于超时阈值误判。
- 打包入口默认上限调整为 600 秒，并开放 `-SmokeTimeoutSeconds 60..1800`；超时会清理精确测试进程树，报告记录上限和实际耗时。
- 四个打包 PowerShell 脚本语法检查通过；无需重新构建 MiKTeX Runtime。

### 2026-09-11：顺序执行要求落地

- 核对开发要求与既有 M0、M1、M2 验收记录，建立本阶段总表。
- 将顺序执行、阶段完成记录和阻塞处理规则加入开发要求第 26 节。
- 调整后续业务链路入口：先完成 M2，之后才推进 M3。
- 本次仅更新文档，不新增功能，不重新运行构建或测试，不新增阶段完成结论。
- 当前停在 M2，等待上述具体安全权限决策；M3～M8 保持待执行。

## 后续阶段记录模板

### 2026-09-11：持续执行授权与 M3 会话核心增量

用户要求不再逐阶段等待指令：完成一个阶段后先写入本地记录，再继续下一阶段，直到 M8。正常范围内的实现与测试无需重新确认；不得自动扩大安全权限、下载 TeX 或把未验收阶段标为完成。

本次新增 KRpcSession，提供 64 个请求／终态事件的总容量限制、256 条已完成 ID 窗口、协作取消、超时、单调事件序号和代际 Ticket。取消只请求停止，必须收到任务完成确认才发布终态。Debug／Release 纯核心全新 Configure、Build 与 CTest 均通过，各 8/8。详细记录见 M3 验收文档。

当前实际状态仍为 M3 进行中：会话核心尚未接入 CEF 异步调用，事件／取消 wire 契约和 Renderer 会话释放仍待完成。不能按此增量启动 M4。

后续环境风险：本次检查 PATH、已知依赖目录及常见安装目录均未发现 TeX。后续真实编译验收需要用户已有工具链路径或明确安装授权；这不是当前 M3 核心实现的阻塞，不以此冒充已经完成 M5。

每完成一阶段，在 `docs/acceptance/` 创建或更新对应记录，并同步本表。失败或阻塞也及时记录，不等到阶段完成才说明。

```markdown
# Mx 阶段验收记录

- 日期：
- 状态：进行中／阻塞／已验收
- 对应需求与本阶段范围：
- 实际实现与涉及文件：
- 模块边界、依赖方向和替换验证：
- 环境、工作目录、实际命令、退出码与结果：
- 正常／失败／取消／超时／资源释放验证（不适用须说明）：
- UI 或运行证据位置：
- 未验证项、已知风险和阻塞：
- 完成结论及依据：
- 下一阶段入口条件：
```
### 2026-09-13：M8 切换为 Full 绿色目录、运行时零解压

- 按用户最终要求取消 Lite、Portable TeX Live 和单文件自解压发布，仅保留源码版 MiKTeX Full Windows x64 绿色目录。
- 删除 `package-lite.ps1`、`package-onefile.ps1` 与 `LightOverLeafRuntimeLauncher`；统一 `package.ps1` 直接复制目录并生成 SHA-256 清单，不依赖 7-Zip、tar 或 IExpress。
- Full 装配检测到 `runtime/miktex` 后，Build 与 SyncTeX 均关闭系统 PATH/外部 Root 回退；新增 `texmfs/install/miktex/bin/x64` SyncTeX 布局回归。
- 前端 288/288、Release CTest 33/33 通过；最终目录 smoke 2028 ms，GUI Subsystem 检查通过，无禁止归档/启动器文件。
- 最终内置 XeLaTeX 真实生成 5172-byte PDF、538-byte SyncTeX 和 2866-byte 日志；证据位于 `out/acceptance/full-green-20260913-194005`。
- 产物：`out/distributions/full-20260913-194005-8fe4bca6/LightOverLeaf-Full-win64`。代码签名、干净 Windows 虚拟机人工流程和 complete 宏包集合仍待补。

### 2026-09-13：M8 草稿保存并编译入口修复

- 修复原生目录选择器使用空父窗口导致可能隐藏到主窗口后方的问题；主窗口通过 `QPointer` 受控观察，生命周期结束即清空。
- 前端加入独立可测试的 `preparing` 状态、重复点击保护，以及取消、非空目录、导入失败的明确反馈。
- 本轮实际验证：前端 291/291、Release 构建、CTest 33/33、Full 目录 smoke 均通过。
- 新产物：`out/distributions/full-20260913-202937-c0a407a0/LightOverLeaf-Full-win64`；新目录内置 XeLaTeX 实编译证据位于 `out/acceptance/compile-ui-fix-20260913-202937`。
- 自动化已确认真实 PDF/SyncTeX 生成；Windows 原生选择器的可见层级仍需用户在新 EXE 上人工点击确认。

### 2026-09-13：M9 实时预览与仅导出选择路径方案

- 用户确认新的产品约束：新草稿的手动编译和实时预览均不要求目标目录；仅显式导出/另存为选择目标目录，打开已有项目仍选择源目录，已有项目普通保存不重复询问。
- 新增 ADR 0018 与 M9 开发计划，定义 Draft ID、只读基础快照加 Overlay、Generation、Latest-wins、PDF 原子切换、独立 Export Application 和原生 destinationToken 边界。
- 新增 M9 验收记录骨架。当前产品代码仍使用“草稿选择空目录后保存并编译”的 M8 流程，M9 状态为“规划完成，尚未实现”，不得冒充已经支持无路径实时预览。
- 本次仅修改 Markdown 文档；未修改 C++/React/RPC Schema，未重新运行前端测试、CMake 构建、CTest、MiKTeX 编译或桌面人工验收。
- 本次验证仅执行 Markdown 差异检查、关键引用检查和相对链接存在性检查；后续按 M9.0 到 M9.6 顺序实现，每完成一个子阶段继续追加本地证据。

### 2026-09-14：M9.1/M9.2 无路径草稿编译核心增量

- `build.start` 新增最多 32 文件、单文件 4 MiB、总计 16 MiB 的相对 File ID Overlay；Build Application 做重复/容量/路径校验，Windows Snapshot Store 支持空基础快照或已授权 Workspace 副本叠加，并清理完成、取消和异常产生的临时目录。
- Workspace Workflow 的 Build 服务改为惰性解析，`build.detect` 与草稿 Build 不再要求先打开 Workspace；RPC 只做生成契约映射，Build 与 Workspace/Qt/CEF/React 仍由端口隔离。
- 前端编译直接捕获 Monaco/EditorSession 文件作为 Overlay；顶部入口改为“导出草稿”，只有显式导出选择目标目录。取消/失败导出和失败编译均不清空上一份成功 PDF 字节。
- 草稿与本地项目均加入 900 ms 前端防抖；Build 按钮使用独立 `build` capability，不再耦合 `nativeFiles`。
- 本轮 `desktop-release` Configure/Build 通过；前端 ESLint、291/291、TypeScript、Vite 通过；完整 CTest 33/33 通过，包含架构、非法依赖、代码生成漂移和桌面 CEF smoke。
- 新 Full 绿色目录：`out/distributions/full-20260914-212415-8515f29d/LightOverLeaf-Full-win64`。19562 个清单文件、1438310587 bytes，目录 smoke 1942 ms；无归档、无 7-Zip、零运行时解压，直接运行 `LightOverLeaf.exe`。
- M9 仍为进行中：原生 Generation/Latest-wins 单 Active+Pending、Preferences 三态、PDF.js 加载成功后原子提交、独立 Export Application/destinationToken、可见桌面和干净 Windows 验收尚未完成。详细证据见 [M9 验收记录](../acceptance/M9-live-preview.md)。
### 2026-09-14：M9.0 三态偏好与 Generation 契约

- Preferences 从 `autoCompile` 升级为 `manual/onSave/live`；新数据库默认 `live`，旧 SQLite 布尔值幂等迁移为 `true -> onSave`、`false -> manual`。
- React 设置页改为三态选择；`live` 使用 900 ms 防抖，`onSave` 仅在成功保存/检查点后触发，`manual` 只响应按钮。已有项目实时编译直接使用不可变 Overlay，不再为了预览先写磁盘。
- Build V2 契约加入 `scopeId`、安全整数 `generation` 和 `phase`；托管草稿使用持久化 Draft ID，React/RPC 仍不接触绝对路径。
- 验证：前端 291/291；desktop-release 构建通过；Preferences、Contract、Codegen Drift 3/3 通过。

### 2026-09-14：M9.3 原生 Latest-wins 调度

- Build Application 按草稿/项目 scope 维护一个 Active 和一个合并后的 Pending；新 generation 请求停止 Active，并替换更旧 Pending。
- Pending 被替换或取消会可靠唤醒并返回 cancelled；旧 generation 在 Snapshot 物化前以冲突拒绝；完成状态保留 generation 与 phase。
- React 不再用 `activeBuildJob` 阻止新 generation 进入原生调度；请求序号仍阻止迟到日志、诊断或 Artifact 覆盖最新界面。
- 新增连续三代压力 Fixture：前两代取消，第三代成功且 Artifact generation=3；随后 generation=2 被拒绝。
- 验证：Build、WorkspaceWorkflowRpc、Contract、Architecture、Codegen Drift 5/5 通过；desktop-release 构建通过。

### 2026-09-14：M9.4 PDF.js 双缓冲与结构化阶段

- Build 契约与状态增加 `snapshot/detect/compile/artifact/render/complete` phase 和 generation；日志面板显示失败阶段及旧预览保留状态。
- 新 Artifact 仅进入 `candidate`；PDF.js 在离屏 Canvas 完成加载与首屏渲染后才原子替换可见 Canvas、artifactId、PDF 字节与 SyncTeX 元数据。
- 候选加载或渲染失败会销毁候选 LoadingTask、保留上一份已提交文档，并把失败记录为 render phase；翻页和缩放也先离屏渲染再提交。
- 新增 rendering 状态回归；前端测试 292/292、TypeScript 与 Vite 生产构建通过。

### 2026-09-14：M9.5 独立 Export Application

- 新增独立 Export Domain/Application/Outbound/LocalFS Adapter，不复用 Workspace 或 Document 保存用例。
- Qt 目录选择只生成绑定 CEF session 的一次性 `destinationToken`；React/RPC 不接触绝对路径，端点关闭即时撤销未使用授权。
- 草稿导出提交不可变 Generation 快照；Adapter 在同父目录 staging 完整写入并 flush 后原子重命名，取消和失败不改变目标目录。
- Export C++ 测试和前端 Export RPC 测试通过，覆盖跨会话、过期、重复、非空目录、穿越路径、取消与路径不泄漏。

### 2026-09-14：M9.6 完整验收与最终 Full 目录

- 旧 SQLite `auto_compile` 真实迁移 fixture、Build V2 Generation/Phase fixture 均通过。
- 前端 294/294、TypeScript、Vite 通过；Release CTest 34/34 通过，架构检查 92 个 Target。
- 最终 Full 绿色目录：`out/distributions/full-20260914-234447-bf7bcab0/LightOverLeaf-Full-win64`。
- 包内 MiKTeX、GUI smoke 和完整清单验证通过；19562 个文件、1438377026 bytes，无归档、无 7-Zip、启动零解压，直接运行 `LightOverLeaf.exe`。
- M9 产品开发状态更新为“已完成并通过自动化验收”。代码签名、干净 Windows 人工操作和论文宏包矩阵属于外部发布签收事项。