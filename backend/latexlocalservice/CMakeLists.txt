cmake_minimum_required(VERSION 3.21)
project(LightOverLeaf VERSION 0.1.0 LANGUAGES CXX)
get_filename_component(LOL_REPO_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../.." ABSOLUTE)
include(CTest)
include(cmake/ProjectOptions.cmake)
include(cmake/ArchitectureRules.cmake)
find_package(Python3 REQUIRED COMPONENTS Interpreter)
include(cmake/Contracts.cmake)
# Local LaTeX service; dependency edges remain enforced by target metadata.
add_subdirectory(app/kernel)
add_subdirectory(app/system)
add_subdirectory(workspace/project)
add_subdirectory(workspace/document)
add_subdirectory(workspace/search)
add_subdirectory(texengine/build)
add_subdirectory(workspace/export)
add_subdirectory(texengine/preview)
add_subdirectory(texengine/navigation)
add_subdirectory(persistence/preferences)
add_subdirectory(persistence/session)
add_subdirectory(workspace/workflow)
add_subdirectory(transport/rpc)
add_subdirectory(transport/loopback)
if(NOT LIGHTOVERLEAF_BUILD_ELECTRON)
  add_subdirectory(transport/cef)
endif()
add_subdirectory(app/platform)
if(LIGHTOVERLEAF_BUILD_ELECTRON)
  add_subdirectory(transport/json)
endif()
add_subdirectory(app/composition)
if(LIGHTOVERLEAF_BUILD_ELECTRON)
  add_subdirectory(app/backend)
endif()
if(LIGHTOVERLEAF_BUILD_DESKTOP)
  include(${PROJECT_SOURCE_DIR}/cmake/Dependencies.cmake)
  include(${PROJECT_SOURCE_DIR}/cmake/Frontend.cmake)
  include(${PROJECT_SOURCE_DIR}/cmake/CefBootstrap.cmake)
  add_subdirectory(app/bootstrap)
endif()
if(BUILD_TESTING)
  add_subdirectory(app/tests)
endif()

lol_export_architecture()
